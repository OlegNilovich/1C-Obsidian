```bsl
// Изменить статусы выделенных заказов

&НаКлиенте
Процедура ИзменитьСтатусыВыделенныхЗаказов(Команда)
	
	МассивЗаказов = Элементы.СписокЗаказов.ВыделенныеСтроки;
	ИзменитьСтатусыВыделенныхЗаказовНаСервере(МассивЗаказов);
	Элементы.СписокЗаказов.Обновить();
	
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ИзменитьСтатусыВыделенныхЗаказовНаСервере(МассивЗаказов)
	
	Для каждого ТекЗаказ Из МассивЗаказов Цикл
		ДокОбъект = ТекЗаказ.ПолучитьОбъект();
		ДокОбъект.Статус = Справочники.СтатусыЗаказовКлиентов.Завершен;
		ДокОбъект.Записать();
	КонецЦикла;
	
КонецПроцедуры
```
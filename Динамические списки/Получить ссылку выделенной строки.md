```bsl
// 1. Выделяем одну строку в динамическом списке
// 2. Получаем одну ссылку выделенного элемента
&НаКлиенте
Процедура ПолучитьСсылкуВыделеннойСтроки(Команда)
	
	СсылкаНаТовар = Элементы.Товары.ТекущаяСтрока;
	ИзменитьВидыТовароа(СсылкаНаТовар);

КонецПроцедуры

// 3. Для обработки ссылки, требуется процедура без контекста!
&НаСервереБезКонтекста
Процедура ИзменитьВидыТовароа(СсылкаНаТовар)
	
	ТоварОбъект = СсылкаНаТовар.ПолучитьОбъект();
	ТоварОбъект.ВидНоменклатуры = Перечисления.ВидыНоменклатуры.Услуга;
	ТоварОбъект.Записать();

КонецПроцедуры
```
#ЗАПРОСЫ

```bsl
&НаКлиенте
Процедура ЗаполнитьТаблицуЗначений(Команда)
	ЗаполнитьТаблицуЗначенийНаСервере();
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьТаблицуЗначенийНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасходнаяНакладнаяСостав.Ссылка.Покупатель КАК		Покупатель,
		|	РасходнаяНакладнаяСостав.Товар КАК							Товар,
		|	РасходнаяНакладнаяСостав.Количество КАК					Количество
		|ИЗ
		|	Документ.РасходнаяНакладная.Состав КАК РасходнаяНакладнаяСостав";
	
	// 1 Способ
	РезультатТаблицаЗначений = Запрос.Выполнить().Выгрузить();
	ТаблицаЗначенийНаФорме.Загрузить(РезультатТаблицаЗначений);
	
	// 2 Способ
	РезультатТаблицаЗначений = Запрос.Выполнить().Выгрузить();
	ЗначениеВДанныеФормы(РезультатТаблицаЗначений, ТаблицаЗначенийНаФорме);

	// 3 Способ
	РезультатТаблицаЗначений = Запрос.Выполнить().Выгрузить();
	ЗначениеВРеквизитФормы(РезультатТаблицаЗначений, "ТаблицаЗначенийНаФорме");
	
	// 4 Способ
	РезультатВыборка = Запрос.Выполнить().Выбрать();
	Пока РезультатВыборка.Следующий() Цикл
		НоваяСтрока = ТаблицаЗначенийНаФорме.Добавить();
		ЗаполнитьЗначенияСвойств(НоваяСтрока, РезультатВыборка);
	КонецЦикла;

	// 5 Способ
	РезультатВыборка = Запрос.Выполнить().Выбрать();
	Пока РезультатВыборка.Следующий() Цикл
		НоваяСтрока					= ТаблицаЗначенийНаФорме.Добавить();
		НоваяСтрока.Покупатель	= РезультатВыборка.Покупатель;
		НоваяСтрока.Товар			= РезультатВыборка.Товар;
		НоваяСтрока.Количество	= РезультатВыборка.Количество;
	КонецЦикла;
	
КонецПроцедуры
```
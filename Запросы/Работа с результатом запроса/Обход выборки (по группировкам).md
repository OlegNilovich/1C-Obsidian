#ЗАПРОСЫ
###### По группировке: "Номенклатура"
```bsl
&НаКлиенте
Процедура ОднаГруппировка(Команда)
	ОднаГруппировкаНаСервере();
КонецПроцедуры
&НаСервере
Процедура ОднаГруппировкаНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасходнаяНакладнаяТовары.Номенклатура КАК		Номенклатура,
		|	РасходнаяНакладнаяТовары.Количество КАК			Количество
		|ИЗ
		|	Документ.РасходнаяНакладная.Товары КАК			РасходнаяНакладнаяТовары
		|ИТОГИ
		|	СУММА(Количество)
		|ПО
		|	Номенклатура";
	
	Выборка = Запрос.Выполнить().Выбрать(ОбходРезультатаЗапроса.ПоГруппировкам);
	Пока Выборка.Следующий() Цикл
		Сообщить("Товар: " + Выборка.Номенклатура + " Количество Итог: " + Выборка.Количество);
	КонецЦикла;

КонецПроцедуры
```
###### По группировкам: "Номенклатура" и "Контрагент"
```bsl
&НаКлиенте
Процедура ДвеГруппировки(Команда)
	ДвеГруппировкиНаСервере();
КонецПроцедуры
&НаСервере
Процедура ДвеГруппировкиНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасходнаяНакладнаяТовары.Ссылка.Контрагент КАК		Контрагент,
		|	РасходнаяНакладнаяТовары.Номенклатура КАК				Номенклатура,
		|	РасходнаяНакладнаяТовары.Количество КАК					Количество
		|ИЗ
		|	Документ.РасходнаяНакладная.Товары КАК					РасходнаяНакладнаяТовары
		|ИТОГИ
		|	СУММА(Количество)
		|ПО
		|	Контрагент,
		|	Номенклатура";
	
	ТипОбхода = ОбходРезультатаЗапроса.ПоГруппировкам;
	ВыборкаКонтрагент = Запрос.Выполнить().Выбрать(ТипОбхода, "Контрагент");
	Пока ВыборкаКонтрагент.Следующий() Цикл
		Текст = "Контрагент: %1, Количество итог: %2";
		Сообщить(СтрШаблон(Текст, ВыборкаКонтрагент.Контрагент, ВыборкаКонтрагент.Количество));
		
		ВыборкаНоменклатура = ВыборкаКонтрагент.Выбрать(ТипОбхода, "Номенклатура");
		Пока ВыборкаНоменклатура.Следующий() Цикл
			Текст = "==> Номенклатура: %1, Количество итог: %2";
			Сообщить(СтрШаблон(Текст, ВыборкаНоменклатура.Номенклатура, 
			ВыборкаНоменклатура.Количество));
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

```
###### По группировкам: "Номенклатура" и "Контрагент" и детальным записям
```bsl
&НаКлиенте
Процедура ДвеГруппировкиИДетальныеЗаписи(Команда)
	ДвеГруппировкиИДетальныеЗаписиНаСервере();
КонецПроцедуры
&НаСервере
Процедура ДвеГруппировкиИДетальныеЗаписиНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	РасходнаяНакладнаяТовары.Ссылка.Контрагент КАК		Контрагент,
		|	РасходнаяНакладнаяТовары.Номенклатура КАК				Товар,
		|	РасходнаяНакладнаяТовары.Количество КАК					Количество
		|ИЗ
		|	Документ.РасходнаяНакладная.Товары КАК					РасходнаяНакладнаяТовары
		|ИТОГИ
		|	СУММА(Количество)
		|ПО
		|	Контрагент,
		|	Товары";
	
	ПоГруппировкам = ОбходРезультатаЗапроса.ПоГруппировкам;
	ВыборкаКонтрагент = Запрос.Выполнить().Выбрать(ПоГруппировкам, "Контрагент");
	Пока ВыборкаКонтрагент.Следующий() Цикл
		Текст = "Контрагент: %1, Количество итог: %2";
		Сообщить(СтрШаблон(Текст, ВыборкаКонтрагент.Контрагент, ВыборкаКонтрагент.Количество));
		
		ВыборкаТовары = ВыборкаКонтрагент.Выбрать(ПоГруппировкам, "Товары");
		Пока ВыборкаТовары.Следующий() Цикл
			Текст = "=> Товар: %1, Количество итог: %2";
			Сообщить(СтрШаблон(Текст, ВыборкаТовары.Товар, ВыборкаТовары.Количество));
			
			ВыборкаДетали = ВыборкаТовары.Выбрать();
			Пока ВыборкаДетали.Следующий() Цикл
				Текст = "==> Товар: %1, Количество: %2";
				Сообщить(СтрШаблон(Текст, ВыборкаДетали.Товар, ВыборкаДетали.Количество));
			КонецЦикла;
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

```

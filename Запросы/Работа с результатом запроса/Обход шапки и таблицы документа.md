#ЗАПРОСЫ 

```bsl
Запрос = Новый Запрос;
Запрос.Текст = 
	"ВЫБРАТЬ
	|	РасходнаяНакладная.Дата,
	|	РасходнаяНакладная.Покупатель,
	|	РасходнаяНакладная.Товары.(
	|		Товар,
	|		Количество
	|	) КАК Товары
	|ИЗ
	|	Документ.РасходнаяНакладная КАК РасходнаяНакладная";

ВыборкаШапка = Запрос.Выполнить().Выбрать();
Пока ВыборкаШапка.Следующий() Цикл
	Сообщить("Покупатель:  " + ВыборкаШапка.Покупатель + ".  Дата покупки: " + ВыборкаШапка.Дата);
	
	ВыборкаТовары = ВыборкаШапка.Товары.Выбрать();
	Пока Товары.Следующий() Цикл
		Сообщить("==> Товар:  " + ВыборкаТовары.Товар + "  " + ВыборкаТовары.Количество + " шт.");
	КонецЦикла;
КонецЦикла;
```
Диалог подтверждения действия пользователем

```bsl
&НаКлиенте
Процедура СпроситьУПользователя(Команда)
	
	Оповещение = Новый ОписаниеОповещения("СпроситьУПользователяЗавершение", ЭтаФорма, Неопределено);
	ТекстВопроса = "Будет выполнено некое действие. Продолжить?";
	ПоказатьВопрос(Оповещение, ТекстВопроса, РежимДиалогаВопрос.ДаНет);
	
КонецПроцедуры

&НаКлиенте
Процедура СпроситьУПользователяЗавершение(Результат, ДополнительныеПараметры = Неопределено) Экспорт
	
	Если Результат = КодВозвратаДиалога.Нет Тогда
		Возврат;
	КонецЕсли;
	
    // Сделать что нужно
	
КонецПроцедуры
```
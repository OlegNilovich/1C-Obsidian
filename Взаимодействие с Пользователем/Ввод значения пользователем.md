###### Первый способ: ВвестиЗначениеАсинх
```bsl
&НаКлиенте
Асинх Процедура МетодВводЗначения(Команда)
	
	Подсказка	= "Выберите контрагента";
	Тип			= Тип("СправочникСсылка.Контрагенты");
	Значение	= Ждать ВвестиЗначениеАсинх(, Подсказка, Тип);
	
	Если Значение <> Неопределено Тогда
		Сообщить("Вы выбрали: " + Значение);
	КонецЕсли; 
	
КонецПроцедуры
```

###### Второй способ: ПоказатьВводЗначения
```bsl
&НаКлиенте
Процедура МетодВводЗначения(Команда)
	Оповещение	= Новый ОписаниеОповещения("ПослеВводаЗначения", ЭтотОбъект);
	Подсказка		= "Выберите контрагента";
	Тип				= Тип("СправочникСсылка.Контрагенты");
	ПоказатьВводЗначения(Оповещение,, Подсказка, Тип);
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаЗначения(Значение, ДополнительныеПараметры) Экспорт
	Если Значение <> Неопределено Тогда
		Сообщить("Вы выбрали: " + Значение);
	КонецЕсли; 
КонецПроцедуры
```
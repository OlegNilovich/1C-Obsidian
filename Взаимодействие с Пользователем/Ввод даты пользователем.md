###### Первый способ: ВвестиЗначениеАсинх
```bsl
&НаКлиенте
Асинх Процедура МетодВвестиДату(Команда)
	
	Подсказка = "Введите дату";
	ЧастьДаты = ЧастиДаты.Дата;
	Дата = Ждать ВвестиДатуАсинх(, Подсказка, ЧастьДаты);
	
	Если Дата <> Неопределено Тогда
		Сообщить("Указанная дата: " + Дата);
	КонецЕсли;
	
КонецПроцедуры
```

###### Второй способ: ПоказатьВводДаты
```bsl
&НаКлиенте
Процедура ВводДатыАсинхронно(Команда)
	Оповещение = Новый ОписаниеОповещения("ПослеВводаДаты", ЭтотОбъект);
	ПоказатьВводДаты(Оповещение,, "Введите дату доставки");
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаДаты(Дата, ДополнительныеПараметры) Экспорт
	Если Дата <> Неопределено Тогда
		Сообщить(Дата);
	КонецЕсли; 
КонецПроцедуры
```
###### Первый способ: ВвестиСтрокуАсинх
```bsl
&НаКлиенте
Асинх Процедура МетодВвестиСтроку(Команда)
	
	Подсказка				= "Введите строку";
	Длина					= 100;
	Многострочность	= Истина;
	Строка = Ждать ВвестиСтрокуАсинх(, Подсказка, Длина, Многострочность);
	
	Если Строка <> Неопределено Тогда
		Сообщить("Указанная строка: " + Строка);
	КонецЕсли;
	
КонецПроцедуры
```

###### Второй способ: ПоказатьВводСтроки
```bsl
&НаКлиенте
Процедура МетодВводЗначения(Команда)
	Оповещение			= Новый ОписаниеОповещения("ПослеВводаСтроки", ЭтотОбъект);
	Подсказка				= "Введите строку";
	Длина					= 100;
	Многострочность	= Истина;
	ПоказатьВводЗначения(Оповещение,, Подсказка, Тип);
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаСтроки(Значение, ДополнительныеПараметры) Экспорт
	Если Значение <> Неопределено Тогда
		Сообщить("Введенная строка: " + Значение);
	КонецЕсли; 
КонецПроцедуры
```
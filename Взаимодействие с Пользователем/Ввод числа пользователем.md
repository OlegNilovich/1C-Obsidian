###### Первый способ: ВвестиЧислоАсинх
```bsl
&НаКлиенте
Асинх Процедура МетодВвестиЧисло(Команда)
	
	Подсказка	= "Введите число";
	Длина		= 5;
	Точность 	= 2;
	Число = Ждать ВвестиЧислоАсинх(, Подсказка, Длина, Точность);
	
	Если Число <> Неопределено Тогда
		Сообщить("Указанное число: " + Число);
	КонецЕсли;
	
КонецПроцедуры
```

###### Второй способ: ПоказатьВводЧисла
```bsl
&НаКлиенте
Процедура ВводЧислаАсинхронно(Команда)
	ВведенноеЧисло = 0;
	Оповещение = Новый ОписаниеОповещения("ПослеВводаЧисла", ЭтотОбъект);
	ПоказатьВводЧисла(Оповещение, ВведенноеЧисло, "Введите количество товара"); 
КонецПроцедуры

&НаКлиенте
Процедура ПослеВводаЧисла(Число, ДополнительныеПараметры) Экспорт
	Если Число <> Неопределено Тогда
		Сообщить(Число);
	КонецЕсли; 
КонецПроцедуры
```
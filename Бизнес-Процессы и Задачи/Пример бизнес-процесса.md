```bsl
// Пример бизнес-процесса

// 1. Создаем непереодический регистр сведений "РегистрАдресации"
// Создаем Измерение "Исполнитель" (СправочникСсылка.Пользователи)
// Создаем Измерение "Должность" (СправочникСсылка.Должности)

//=======================================================================

// 2. Создаем объект конфигурации "ЗадачаДоработки1С"
// Окно редактирования => Адресация => Адресация => "РегистрАдресации"
// Текущий исполнитель => ТекущийПользователь (Параметр сеанса)
// Создаем реквизит адресации "Исполнитель" (СправочникСсылка.Пользователи)
// Свойства => Адресация => Измерение адресации => Исполнитель
// Основной реквизит адресации => "Исполнитель"
// Создаем реквизит адресации "Должность" (СправочникСсылка.Должности)
// Свойства => Адресация => Измерение адресации => Должность

// Создаем форму объекта "Задача", размещаем нужные реквизиты объекта и бизнес-процесса
// Создаем форму списка "МоиЗадачи". Реквизит "Список" => Свойства =>
// Объект => ОсновнаяТаблица => Задача.ЗадачаДоработки1С.ЗадачиПоИсполнителю

// Создаем команду "ОткрытьФормуМоиЗадачи", Группа (Панель навигации.Важное)
// Модуль команды:
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ОткрытьФорму("Задача.ЗадачаДоработки1С.Форма.ФормаМоиЗадачи");
КонецПроцедуры

// Модуль объекта "ЗадачаДоработки1С":
Процедура ПередВыполнением(Отказ)
	Исполнитель = ПараметрыСеанса.ТекущийПользователь;
КонецПроцедуры


//=======================================================================

// 3.1 Создаем Бизнес-процесс "Доработка1С"
// Окно редактирования => Основные => Задачи => "ЗадачаДоработки1С"
// Создаем реквизиты:
// - Контрагент				(СправочникСсылка.Контрагенты)
// - ОписаниеДоработки		(Строка, 1000)
// - ТехническоеЗадание		(Строка, 1000)
// - ОценкаТрудозатрат		(Число, 5) - В часах
// - ЖелаемыйСрокВыполнения (Дата, Дата)
// - ДоговорПодписан		(Булево)

// 3.2 Создаем карту маршрута:
// Точка старта: "Старт"
// Точка действия: "Поставить задачу". Адресация => Должность => Менеджер
// Точка действия: "Сформировать ТЗ". Адресация => Должность => Аналитик
// Точка действия: "Сформировать ТЗ". Адресация => Должность => Аналитик
// Точка действия: "Оценить сроки". Адресация => Должность => Программист
// Точка действия: "Оформить договор". Адресация => Должность => Менеджер
// Точка условия: "Договор подписан?". События => Проверка условия => Процедура()
// Точка условия: "Договор подписан?" => Нет => Завершение бизнес-процесса
// Точка условия: "Договор подписан?" => Да => Переход к след точке действия
// Точка действия: "Выполнить доработку". Адресация => Должность => Программист
// Точка действия: "ОПротестировать доработку". Адресация => Должность => Тестировщик
// Точка действия: "Сдать работу клиенту". Адресация => Должность => Менеджер
// Точка действия: "Акт и счет на оплату". Адресация => Должность => Бухгалтер
// Точка завершения: "Завершение"

// Модуль объекта бизнес-процесса
Процедура ДоговорПодписанПроверкаУсловия(ТочкаМаршрутаБизнесПроцесса, Результат)
	Результат = ДоговорПодписан;
КонецПроцедуры


// 3.3 Создаем форму бизнес-процесса
// Размещаем необходимые реквизиты объекта на форме
// Создаем реквизит "КартаМаршрута" (ГрафическаяСхема)
// Переносим реквизит на форму "КартаМаршрута"
// Создаем два события формы: "ПриЧтенииНаСервере" и "ПослеЗаписи" &НаКлиенте

// Модуль формы бизнес процесса
&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	ОбновитьКартуМаршрута();
КонецПроцедуры

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	ОбновитьКартуМаршрута();
КонецПроцедуры

&НаСервере
Процедура ОбновитьКартуМаршрута()
	БизнесПроцессОбъект = РеквизитФормыВЗначение("Объект");
	КартаМаршрута = БизнесПроцессОбъект.ПолучитьКартуМаршрута();	
КонецПроцедуры


//4. Визуализация хода бизнес-процесса
//5. Проверка работы бизнес-процесса в режиме пользователя
//6. Настройка адресации задач
//7. Отображение задач по исполнителям. Форма “Мои задачи”
```
```bsl
// Список задач - Kanban доска

// 1.1 Создаем объект дерева конфигурации "Задачи" с названием "СписокЗадачКанбанДоска"
// 1.2 Создаем реквизит: Описание		(Строка, 1000)
// 1.3 Создаем реквизит: Исполнитель	(СправочникСсылка.Пользователи)
// 1.4 Создаем реквизит: СрокВыполнения	(Дата, Дата)
// 1.5 Создаем реквизит: Статус			(ПеречислениеСсылка.СтатусыЗадач)
// 1.6 Статус => Свойства => Представление => Значение Заполнения => НужноСделать

//=======================================================================
// 2.1 Создаем форму задачи
// Отключаем автозаполнение командной панели формы
// Переносим на командную панель стандартную команду формы "Записать"
// Переносим на командную панель стандартную команду формы "Выполнить"
// Элементы => Статус => Свойства => Основные => ВидПереключателя => Тумблер
// Элементы => Описание => Свойства => Основные => МногострочныйРежим => Да
// Элементы => Описание => Свойства => Расположение => Высота => 4
// Элементы => Описание => Свойства => Расположение => АвтоМаксимальнаяШирина => Нет

//=======================================================================

// 3.1 Создаем форму списка задач
// Отключаем автозаполнение командной панели формы
// Переносим на командную панель стандартную команду динамического списка "Создать"
// Создаем группу "ГруппаСписки", Группировка: Горизонтальная всегда (без заголовка)
// Внутри группы "ГруппаСписки" создаем группу "ГруппаНужноВыполнить" (с заголовком)
// Внутри группы "ГруппаСписки" создаем группу "ГруппаВРаботе" (с заголовком)
// Внутри группы "ГруппаСписки" создаем группу "ГруппаТестирование" (с заголовком)
// Внутри группы "ГруппаСписки" создаем группу "ГруппаГотово" (с заголовком)

// 3.2 Создаем реквизит "СписокНужноВыполнить" (ДинамическийСписок)
// Свойства => Объект => ОсновнаяТаблица => Задача.ЗадачаПользователя
// Настройка списка => Отбор => Статус Равно Перечисление.СтатусыЗадач.НужноСделать
// Переносим реквизит в группу "ГруппаНужноВыполнить", с одной колонкой "Наименование"
// Отключаем автозаполнение командной панели динамического списка
// Свойства => События => Перетаскивание => Создаем событие (нажимаем лупу)

// 3.3 Создаем реквизит "СписокВРаботе" (ДинамическийСписок)
// Свойства => Объект => ОсновнаяТаблица => Задача.ЗадачаПользователя
// Настройка списка => Отбор => Статус Равно Перечисление.СтатусыЗадач.ВРаботе
// Переносим реквизит в группу "ГруппаВРаботе", с одной колонкой "Наименование"
// Отключаем автозаполнение командной панели динамического списка
// Свойства => События => Перетаскивание => Создаем событие (нажимаем лупу)

// 3.4 Создаем реквизит "СписокТестирование" (ДинамическийСписок)
// Свойства => Объект => ОсновнаяТаблица => Задача.ЗадачаПользователя
// Настройка списка => Отбор => Статус Равно Перечисление.СтатусыЗадач.Тестирование
// Переносим реквизит в группу "ГруппаТестирование", с одной колонкой "Наименование"
// Отключаем автозаполнение командной панели динамического списка
// Свойства => События => Перетаскивание => Создаем событие (нажимаем лупу)

// 3.5 Создаем реквизит "СписокГотово" (ДинамическийСписок)
// Свойства => Объект => ОсновнаяТаблица => Задача.ЗадачаПользователя
// Настройка списка => Отбор => Статус Равно Перечисление.СтатусыЗадач.Готово
// Переносим реквизит в группу "ГруппаГотово", с одной колонкой "Наименование"
// Отключаем автозаполнение командной панели динамического списка
// Свойства => События => Перетаскивание => Создаем событие (нажимаем лупу)


//=======================================================================
// Модуль формы списка задач (события)

&НаКлиенте
Процедура СписокНужноВыполнитьПеретаскивание(Элемент, ПараметрыПеретаскивания)
	НовыйСтатус = ПредопределенноеЗначение("Перечисление.СтатусыЗадач.НужноСделать");
	ОбновитьСтатусыЗадачКлиент(ПараметрыПеретаскивания.Значение, НовыйСтатус);
КонецПроцедуры

&НаКлиенте
Процедура СписокВРаботеПеретаскивание(Элемент, ПараметрыПеретаскивания)
	НовыйСтатус = ПредопределенноеЗначение("Перечисление.СтатусыЗадач.ВРаботе");
	ОбновитьСтатусыЗадачКлиент(ПараметрыПеретаскивания.Значение, НовыйСтатус);
КонецПроцедуры

&НаКлиенте
Процедура СписокТестированиеПеретаскивание(Элемент, ПараметрыПеретаскивания)
	НовыйСтатус = ПредопределенноеЗначение("Перечисление.СтатусыЗадач.Тестирование");		
	ОбновитьСтатусыЗадачКлиент(ПараметрыПеретаскивания.Значение, НовыйСтатус);
КонецПроцедуры

&НаКлиенте
Процедура СписокГотовоПеретаскивание(Элемент, ПараметрыПеретаскивания)
	НовыйСтатус = ПредопределенноеЗначение("Перечисление.СтатусыЗадач.Готово");		
	ОбновитьСтатусыЗадачКлиент(ПараметрыПеретаскивания.Значение, НовыйСтатус);
КонецПроцедуры


//=======================================================================
// Модуль формы списка задач (вспомогательные процедуры)

&НаКлиенте
Процедура ОбновитьСтатусыЗадачКлиент(МассивЗадач, НовыйСтатус)
	ОбновитьСтатусЗадач(МассивЗадач, НовыйСтатус);		
	ОповеститьОбИзменении(Тип("ЗадачаСсылка.ЗадачаПользователя"));
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ОбновитьСтатусЗадач(МассивЗадач, НовыйСтатус)

	Для каждого ТекЗадача Из МассивЗадач Цикл
		
		ЗадачаОбъект = ТекЗадача.ПолучитьОбъект();

		Если ЗадачаОбъект.Статус = НовыйСтатус Тогда
			Продолжить;
		Иначе
			ЗадачаОбъект.Статус = НовыйСтатус;
		КонецЕсли; 

		Если НовыйСтатус = Перечисления.СтатусыЗадач.Готово Тогда
			ЗадачаОбъект.Выполнена = Истина;
		КонецЕсли; 
		
		ЗадачаОбъект.Записать();
	
	КонецЦикла; 

КонецПроцедуры

```
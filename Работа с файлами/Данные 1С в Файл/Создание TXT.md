```bsl
&НаКлиенте
Асинх Процедура Создать_TXT(Команда)
	
	Текст = Новый ТекстовыйДокумент;
	
	Для каждого Товар Из Товары Цикл
		МассивСлов = Новый Массив;
		МассивСлов.Добавить(Товар.Наименование);
		МассивСлов.Добавить(Товар.ВидНоменклатуры);
		МассивСлов.Добавить(Товар.Артикул);
		МассивСлов.Добавить(Товар.ЕдиницаИзмерения);
		МассивСлов.Добавить(Товар.СтавкаНДС);
		СтрокаФайла = СтрСоединить(МассивСлов, ";");
		Текст.ДобавитьСтроку(СтрокаФайла);
	КонецЦикла;  
	
	ПолноеИмяФайла = МестоВыгрузки + "\" + "TXT-Файл"  + ".txt";
	ЗаписьУспешна = Ждать Текст.ЗаписатьАсинх(ПолноеИмяФайла, КодировкаТекста.UTF8);
	Если ЗаписьУспешна Тогда
		ПоказатьОповещениеПользователя("Данные выгружены успешно!");
		ЗапуститьПриложение(МестоВыгрузки);
	КонецЕсли;
	
КонецПроцедуры
```
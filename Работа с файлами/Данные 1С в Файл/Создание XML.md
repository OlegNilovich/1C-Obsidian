###### Формирование элементов:
```bsl
&НаКлиенте
Процедура Создать_XML(Команда)
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(МестоВыгрузки + "\" + "XML-Файл"  + ".xml");
	
	ЗаписьXML.ЗаписатьОбъявлениеXML(); 
	ЗаписьXML.ЗаписатьНачалоЭлемента("СписокТоваров");
	
	Для каждого Товар Из Товары Цикл
		ЗаписьXML.ЗаписатьНачалоЭлемента("Товар");
		
			ЗаписьXML.ЗаписатьНачалоЭлемента("Наименование");
			ЗаписьXML.ЗаписатьТекст(Товар.Наименование);
			ЗаписьXML.ЗаписатьКонецЭлемента();	

			ЗаписьXML.ЗаписатьНачалоЭлемента("ВидНоменклатуры");
			ЗаписьXML.ЗаписатьТекст(Строка(Товар.ВидНоменклатуры));
			ЗаписьXML.ЗаписатьКонецЭлемента();	
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("Артикул");
			ЗаписьXML.ЗаписатьТекст(Товар.Артикул);
			ЗаписьXML.ЗаписатьКонецЭлемента();	

			ЗаписьXML.ЗаписатьНачалоЭлемента("ЕдиницаИзмерения");
			ЗаписьXML.ЗаписатьТекст(Строка(Товар.ЕдиницаИзмерения));
			ЗаписьXML.ЗаписатьКонецЭлемента();	
			
			ЗаписьXML.ЗаписатьНачалоЭлемента("СтавкаНДС");
			ЗаписьXML.ЗаписатьТекст(Строка(Товар.СтавкаНДС));
			ЗаписьXML.ЗаписатьКонецЭлемента();		
	
		ЗаписьXML.ЗаписатьКонецЭлемента();	
	КонецЦикла;               
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.Закрыть();
	
	ПоказатьОповещениеПользователя("Данные выгружены успешно!");
	ЗапуститьПриложение(МестоВыгрузки);
	
КонецПроцедуры
```

###### Формирование атрибутов:
```bsl
&НаКлиенте
Процедура Создать_XML(Команда)
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.ОткрытьФайл(МестоВыгрузки + "\" + "XML-Файл"  + ".xml");
	
	ЗаписьXML.ЗаписатьОбъявлениеXML(); 
	ЗаписьXML.ЗаписатьНачалоЭлемента("СписокТоваров");
	
	Для каждого Товар Из Товары Цикл
		ЗаписьXML.ЗаписатьНачалоЭлемента("Товар");
		
			ЗаписьXML.ЗаписатьНачалоАтрибута("Наименование");
			ЗаписьXML.ЗаписатьТекст(Товар.Наименование);
			ЗаписьXML.ЗаписатьКонецАтрибута();	

			ЗаписьXML.ЗаписатьНачалоАтрибута("ВидНоменклатуры");
			ЗаписьXML.ЗаписатьТекст(Строка(Товар.ВидНоменклатуры));
			ЗаписьXML.ЗаписатьКонецАтрибута();	
			
			ЗаписьXML.ЗаписатьНачалоАтрибута("Артикул");
			ЗаписьXML.ЗаписатьТекст(Товар.Артикул);
			ЗаписьXML.ЗаписатьКонецАтрибута();	

			ЗаписьXML.ЗаписатьНачалоАтрибута("ЕдиницаИзмерения");
			ЗаписьXML.ЗаписатьТекст(Строка(Товар.ЕдиницаИзмерения));
			ЗаписьXML.ЗаписатьКонецАтрибута();	
			
			ЗаписьXML.ЗаписатьНачалоАтрибута("СтавкаНДС");
			ЗаписьXML.ЗаписатьТекст(Строка(Товар.СтавкаНДС));
			ЗаписьXML.ЗаписатьКонецАтрибута();		
	
		ЗаписьXML.ЗаписатьКонецЭлемента();	
	КонецЦикла;               
	
	ЗаписьXML.ЗаписатьКонецЭлемента();
	ЗаписьXML.Закрыть();
	
	ПоказатьОповещениеПользователя("Данные выгружены успешно!");
	ЗапуститьПриложение(МестоВыгрузки);
	
КонецПроцедуры
```
```bsl
&НаКлиенте
Процедура Загрузить_DBF(Команда)
	
	Товары.Очистить();
	
	ФайлДБФ = Новый XBase;
	ФайлДБФ.ОткрытьФайл(ФайлЗагрузки);
	
	Если НЕ ФайлДБФ.Первая() Тогда
		Сообщить("В выбранном файле нет данных");
		Возврат;
	КонецЕсли;  

	Пока НЕ ФайлДБФ.ВКонце() Цикл
		НоваяСтрока = Товары.Добавить();
		НоваяСтрока.Наименование			= ФайлДБФ.Name;
		НоваяСтрока.ВидНоменклатуры	= ФайлДБФ.Type;
		НоваяСтрока.Артикул					= ФайлДБФ.Article;
		НоваяСтрока.ЕдиницаИзмерения	= ФайлДБФ.Unit;
		НоваяСтрока.СтавкаНДС				= ФайлДБФ.VatRate;
		ФайлДБФ.Следующая();
	КонецЦикла;
	
	ФайлДБФ.ЗакрытьФайл();
	
КонецПроцедуры
```
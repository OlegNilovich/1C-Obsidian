```bsl
&НаКлиенте
Асинх Процедура ОпределитьРасширениеФайла(Команда)
	
	Файл = Новый Файл(ВыбранныйФайл);
	
	Если ЖДАТЬ Файл.СуществуетАсинх() Тогда
		Сообщить("Расширение файла: " + Файл.Расширение);
	Иначе
		Сообщить("Такого файла не существует!");
	КонецЕсли;

КонецПроцедуры
```
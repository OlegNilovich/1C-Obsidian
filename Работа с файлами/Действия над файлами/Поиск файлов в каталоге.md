```bsl
&НаКлиенте
Асинх Процедура ПоискФайловВКаталоге(Команда)
	
	МассивФайлов = Ждать НайтиФайлыАсинх(Каталог, "*.txt", Истина);
	
	Для каждого ТекФайл Из МассивФайлов Цикл
		Если ЖДАТЬ ТекФайл.ЭтоКаталогАсинх() = ЛОЖЬ Тогда
			Имя	 	 = ТекФайл.Имя;
			Размер	 = ЖДАТЬ ТекФайл.РазмерАсинх() / 1024;
			Сообщить(СтрШаблон("Имя: %1  Размер: %2 килобайт", Имя, Размер));
		КонецЕсли;
	КонецЦикла;

КонецПроцедуры
```
```bsl
&НаКлиенте
Асинх Процедура ПроверитьСуществованиеФайла(Команда)
	
	Файл = Новый Файл(ВыбранныйФайл);

	Если ЖДАТЬ Файл.СуществуетАсинх() Тогда
		Сообщить("Все хорошо, файл на месте");
	Иначе
		Сообщить("Такого файла не существует!");
	КонецЕсли;

КонецПроцедуры
```
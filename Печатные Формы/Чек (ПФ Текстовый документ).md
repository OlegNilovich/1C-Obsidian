
##### Модуль команды:
```bsl

Процедура ПечатьЧека(ТекстДок, Ссылка) Экспорт
	
	// Получаем данные для чека
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	РегистрацияЗаказа.Дата КАК Дата,
	|	РегистрацияЗаказа.Номер КАК Номер,
	|	РегистрацияЗаказа.Официант КАК Официант,
	|	РегистрацияЗаказа.Статус КАК Статус,
	|	РегистрацияЗаказа.Стол КАК Стол,
	
	|	РегистрацияЗаказа.СписокБлюд.(
	|		Блюдо КАК Блюдо,
	|		Количество КАК Колво,
	|		Цена КАК Цена,
	|		Сумма КАК Сумма
	|	) КАК СписокБлюд,
	
	|	РегистрацияЗаказа.СуммаДокумента КАК ИтогоСумма,
	|	РегистрацияЗаказа.СпособОплаты КАК СпособОплаты
	
	|ИЗ
	|	Документ.РегистрацияЗаказа КАК РегистрацияЗаказа
	|ГДЕ
	|	РегистрацияЗаказа.Ссылка В(&Ссылка)";
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Выборка = Запрос.Выполнить().Выбрать();
	Выборка.Следующий();
	
	// Получаем макет
	Макет = Документы.РегистрацияЗаказа.ПолучитьМакет("ПФ_TXT_Чек");
	ТекстДок.Очистить();
	
	// Получаем области макета
	ОбластьШапка				= Макет.ПолучитьОбласть("Шапка");
	ОбластьШапкаТаблицы	= Макет.ПолучитьОбласть("ШапкаТаблицы");
	ОбластьСтрокаТаблицы	= Макет.ПолучитьОбласть("СтрокаТаблицы");
	ОбластьПодвал				= Макет.ПолучитьОбласть("Подвал");
	
	// Выводим шапку чека
	ОбластьШапка.Параметры.Номер = Число(Выборка.Номер);
	ОбластьШапка.Параметры.Дата = Формат(Выборка.Дата,"ДФ='dd.MM.yyyy ЧЧ:мм'");
	ОбластьШапка.Параметры.Стол = Выборка.Стол;
	ОбластьШапка.Параметры.Официант = Выборка.Официант;
	ТекстДок.Вывести(ОбластьШапка);
	
	// Выводим шапку таблицы чека
	ТекстДок.Вывести(ОбластьШапкаТаблицы);
	
	// Выводим строки таблицы чека
	ВыборкаСписокБлюд = Выборка.СписокБлюд.Выбрать();
	Пока ВыборкаСписокБлюд.Следующий() Цикл
		ОбластьСтрокаТаблицы.Параметры.Блюдо = ВыборкаСписокБлюд.Блюдо;
		ОбластьСтрокаТаблицы.Параметры.Колво = Формат(ВыборкаСписокБлюд.Колво,"ЧДЦ=3");
		ОбластьСтрокаТаблицы.Параметры.Цена = Формат(ВыборкаСписокБлюд.Цена,"ЧДЦ=2");
		ОбластьСтрокаТаблицы.Параметры.Сумма = Формат(ВыборкаСписокБлюд.Сумма,"ЧДЦ=2");
		ТекстДок.Вывести(ОбластьСтрокаТаблицы);
	КонецЦикла;
	
	// Выводим подвал чека
	ОбластьПодвал.Параметры.ИтогоСумма = Формат(Выборка.ИтогоСумма,"ЧДЦ=2");
	ОбластьПодвал.Параметры.СпособОплаты = Выборка.СпособОплаты;
	ТекстДок.Вывести(ОбластьПодвал);
	
КонецПроцедуры
```

##### Макет:
```bsl

#Область Шапка				
--------------------------------------------------------------
Чек:  [Номер]                      [                     Дата]
Стол: [Стол             ]              [             Официант]
#КонецОбласти


#Область ШапкаТаблицы
--------------------------------------------------------------
Наименование                 | Кол-во |   Цена   |   Сумма
--------------------------------------------------------------
#КонецОбласти


#Область СтрокаТаблицы
[Блюдо                      ]|[ Колво]|[    Цена]|[     Сумма]
#КонецОбласти


#Область Подвал
--------------------------------------------------------------
==============================================================
Итого: [ИтогоСумма        ]
==============================================================
Способ оплаты:                            [      СпособОплаты]
#КонецОбласти	

```
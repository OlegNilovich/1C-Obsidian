```bsl
// Передача данных в подчиненную форму (Параметры формы)

// 1. Создаем главную форму
// 2. Создаем реквизит "ВидНоменклатуры" (СправочникСсылка.ВидыНоменклатуры)
// 3. Создаем команду главной формы:
&НаКлиенте
Процедура ОткрытьПодчиненнуюФормуТоваровСОтборомПоВиду(Команда)

	ПараметрыФормы = Новый Структура("ВидНоменклатуры", ВидНоменклатуры);
	
	ОткрытьФорму("Обработка.Т46_ПередачаДанныхВПодчиненнуюФорму.Форма.ПодчиненнаяФорма",
			ПараметрыФормы, ЭтотОбъект,,,,,РежимОткрытияОкнаФормы.БлокироватьОкноВладельца);

КонецПроцедуры

//=================================================================

// Можно не создавать параметр "ВидНоменклатуры"(СправочникСсылка.ВидыНоменклатуры) в пункте 6.
// Но тогда мы не сможем открыть подчиненную форму без передачи параметра, будет ошибка.

// 4. Создаем подчиненную форму
// 5. Создаем реквизит "Товары" (ДинамическийСписок)
// 6. Создаем параметр "ВидНоменклатуры" (СправочникСсылка.ВидыНоменклатуры)
// 6. Реквизит "Товары" => Палитра свойств => Объект => Произвольный Запрос
// 7. Устанавливаем параметр для запроса динамического списка: &ВидНоменклатуры
ВЫБРАТЬ
	СправочникНоменклатура.Ссылка			КАК Ссылка,
	СправочникНоменклатура.Артикул			КАК Артикул,
	СправочникНоменклатура.СтавкаНДС		КАК СтавкаНДС,
	СправочникНоменклатура.АдресНаСайте		КАК АдресНаСайте,
	СправочникНоменклатура.ВидНоменклатуры	КАК ВидНоменклатуры,
	СправочникНоменклатура.ТипНоменклатуры	КАК ТипНоменклатуры
ИЗ
	Справочник.Номенклатура КАК СправочникНоменклатура
ГДЕ
	СправочникНоменклатура.ВидНоменклатуры = &ВидНоменклатуры
	И НЕ СправочникНоменклатура.ЭтоГруппа

// 8. Создаем событие формы "ПриСозданииНаСервере"
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// 9. Устанавливаем параметр в текст запроса динамического списка
	Если Параметры.Свойство("ВидНоменклатуры") Тогда
		Товары.Параметры.УстановитьЗначениеПараметра("ВидНоменклатуры", Параметры.ВидНоменклатуры);
	КонецЕсли;
	
КонецПроцедуры

```
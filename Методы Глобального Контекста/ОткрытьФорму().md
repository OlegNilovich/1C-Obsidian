#TODO 

```bsl
&НаКлиенте
Процедура ОткрытьФормуНовогоДокумента(Команда)
	ОткрытьФорму("Документ.УстановкаЦен.Форма.ФормаДокумента");
КонецПроцедуры


&НаКлиенте
Процедура ОткрытьФормуКонкретногоДокумента(Команда)
	СсылкаНаДокумент = Новый Структура("Ключ", ДокументУстановкаЦен);
	ОткрытьФорму("Документ.УстановкаЦен.Форма.ФормаДокумента", СсылкаНаДокумент);
КонецПроцедуры


////////////////////////////////////////////////////////////////////////////////////////////
//// Программное открытие второй формы с передачей в нее параметров, из перфой формы


// Модуль первой формы (Реквизиты: Расстояние, Время)
&НаКлиенте
Процедура РасчитатьСкорость(Команда)
	
	ПараметрыФормы = Новый Структура("Расстояние, Время", Расстояние, Время);

	// Нужно для того чтобы каждый раз открывалась новый экземпляр второй формы
	НоваяУникальность = Новый УникальныйИдентификатор;

	ОткрытьФорму(	"Обработка.ОткрытиеФорм.Форма.ФормаРасчетаСкорости",
					ПараметрыФормы, ЭтаФорма, НоваяУникальность);
	
КонецПроцедуры


// Модуль второй формы (Реквизит: Скорость, Параметры: Расстояние, Время)
&НаКлиенте
Процедура ПриОткрытии(Отказ)

	ТекущийВладелец = ЭтаФорма.ВладелецФормы;
	Сообщить("Владелец: " + ТекущийВладелец.Заголовок)

КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	ЗакрыватьПриЗакрытииВладельца = Истина;
	Скорость = Параметры.Расстояние / Параметры.Время;

КонецПроцедуры
```
**Первый вариант:**
```bsl
Попытка
	НоменклатураОбъект = Объект.Номенклатура.ПолучитьОбъект();
	НоменклатураОбъект.Заблокировать();	
Исключение
	Сообщить("Не удалось заблокировать номенклатуру: " + ОписаниеОшибки());
	Возврат;
КонецПопытки;

НоменклатураОбъект.Артикул = "12354";
НоменклатураОбъект.Записать(); 
```

**Второй вариант:**
```bsl
Попытка
	ЗаблокироватьДанныеДляРедактирования(Объект.Номенклатура);
Исключение
	Сообщить("Не удалось заблокировать номенклатуру: " + ОписаниеОшибки());
	Возврат;
КонецПопытки;

НоменклатураОбъект = Объект.Номенклатура.ПолучитьОбъект();
НоменклатураОбъект.Артикул = "12354";
НоменклатураОбъект.Записать();
```